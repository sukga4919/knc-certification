# K&C인증원 홈페이지 사용설명서

처음 만드는 분도 따라 할 수 있게 정리했습니다. 순서대로 보시면 됩니다.

---

## 1. 파일 구성

| 파일 | 역할 | 내가 건드릴 일 |
|------|------|----------------|
| `content.md` | **홈페이지에 들어가는 모든 글자** | ⭐ 여기만 고치면 됩니다 |
| `index.html` | 화면 뼈대 | 거의 안 건드림 |
| `style.css` | 색상·글꼴·디자인 | 색 바꿀 때만 |
| `script.js` | 동작(메뉴·애니메이션 등) | 안 건드림 |

---

## 2. 내용 수정하기 (코딩 필요 없음)

1. `content.md` 파일을 메모장이나 VS Code로 엽니다.
2. `항목명: 내용` 에서 **콜론(:) 뒤의 글자만** 바꿉니다.
   - 예) `회사명: K&C인증원` → `회사명: (주)한빛인증`
3. 줄을 바꾸고 싶으면 내용 안에 `\n` 을 넣습니다.
   - 예) `히어로_제목: 첫째 줄\n둘째 줄`
4. 맨 앞에 `#` 이 붙은 줄은 설명(메모)이라 화면에 안 나옵니다. 지우지 않아도 됩니다.
5. **주의:** 항목명(콜론 왼쪽 글자)과 `[대괄호]` 표시는 지우거나 바꾸지 마세요.

### 사업분야 수정 (그룹 + 인증 항목)
사업분야는 **그룹(국내인증·해외인증·기업(확인)인증)** 과 그 안의 **인증 항목**으로 이루어집니다.

1. 그룹은 `[사업분야그룹]` 아래에 `이름 | 영문 | 설명` 형식으로 적습니다.
   ```
   국내인증 | DOMESTIC | 국내 판매·유통에 필요한 필수 인증
   ```
2. 그 그룹 이름과 **똑같은 이름의 대괄호 블록**에 인증 항목을 넣습니다. 형식은 `마크ID | 인증이름 | 설명` 입니다.
   ```
   [국내인증]
   KC | 국내 KC인증 | 전기·전자·생활용품 등 KC 안전인증과 전자파(EMC) 대응
   ```
3. 항목을 빼려면 그 줄을 지우고, 늘리려면 같은 형식으로 한 줄 추가하면 됩니다.

**마크ID**는 어떤 로고 그림을 보여줄지 고르는 코드입니다. 사용 가능한 코드:
`KC, ENERGY, HIGHEFF, SAFETY, CE, FCC, PSE, UL, CCC, ISO, KS, MAINBIZ, INNOBIZ, VENTURE, RND, DIRECT`

### 실제 공식 로고 이미지 넣기 (선택)
`index.html` 과 같은 폴더에 **`marks` 폴더**를 만들고, 마크ID를 소문자로 한 파일명으로 이미지를 저장하면 자동으로 표시됩니다.
- 예) `marks/kc.png`, `marks/ce.png`, `marks/iso.png`, `marks/mainbiz.png` …
- 파일이 없으면 임시로 그린 대체 그림이 나옵니다. (지금 상태)

### 메뉴 하위목록(마우스 올리면 나오는 목록) 수정
`content.md` 의 `[회사소개_하위]`, `[오시는길_하위]`, `[문의하기_하위]` 부분에서 바꿉니다.
형식은 `보이는 이름: 이동할 곳` 이고, **콜론 왼쪽(이름)만** 바꾸면 됩니다.
- 항목을 빼려면 그 줄을 지우고, 늘리려면 같은 형식으로 한 줄 추가합니다.
- 그 메뉴의 목록을 아예 없애려면 해당 항목들을 모두 지우면 됩니다.
- '사업분야' 메뉴의 목록은 `[사업분야그룹]` 에서 **자동으로** 만들어지므로 따로 없습니다.

---

## 3. 색상 바꾸기 (선택)

`style.css` 파일 맨 위 `:root { ... }` 안의 색상 코드(`#`으로 시작)만 바꾸면 전체 색이 바뀝니다.
`--brand` 가 메인 남색, `--brand-2` 가 포인트 파랑, `--brand-3` 이 보조 청록색입니다.

---

## 4. 내 컴퓨터에서 미리보기

⚠️ **중요:** `index.html` 을 그냥 더블클릭하면 브라우저 보안 정책 때문에 `content.md` 를 읽지 못합니다.
이때는 화면 맨 위에 노란 안내 띠가 뜨고 **예비 내용**이 표시됩니다(= 수정한 내용이 안 보임).
아래처럼 **간단한 서버로 열어야** content.md 수정 내용이 그대로 반영됩니다.

**방법 A — Claude Code에게 맡기기 (가장 쉬움)**
> "이 폴더를 로컬 서버로 띄워서 미리보기 주소 알려줘"

**방법 B — 직접 하기 (컴퓨터에 파이썬이 있는 경우)**
1. 이 폴더에서 터미널(명령창)을 엽니다.
2. `python -m http.server` 입력 후 엔터.
3. 브라우저에서 `http://localhost:8000` 접속.

> 배포(5번)를 하고 나면 실제 주소에서는 항상 정상 표시되니, 미리보기가 번거로우면 바로 배포해서 확인해도 됩니다.

---

## 5. 무료로 인터넷에 올리기 (배포)

가장 쉬운 방법은 **Netlify Drop** 입니다. 회원가입도 파일을 끌어다 놓기만 하면 됩니다.

1. `app.netlify.com/drop` 접속
2. **이 폴더(company-site) 전체**를 화면 위로 드래그 앤 드롭
3. 잠시 뒤 `랜덤이름.netlify.app` 주소가 생성됩니다. 끝!

이후 내용을 바꿨을 때는 수정한 폴더를 같은 곳에 다시 올리면 갱신됩니다.
자동 배포·전용 도메인 연결까지 원하면 Claude Code에게:
> "이 프로젝트를 GitHub에 올리고 Netlify에 연결해서 자동 배포되게 해줘"
라고 요청하면 단계별로 도와줍니다.

---

## 6. 문의 폼 안내

- **기본값**: 방문자가 폼을 작성하면 `content.md` 의 `이메일` 주소로 **메일 작성 창**이 열립니다. 별도 설정 없이 바로 동작합니다.
- **홈페이지에서 바로 접수받고 싶다면**: [formspree.io](https://formspree.io) (무료)에 가입 → 받은 주소(예: `https://formspree.io/f/abcdwxyz`)를 `content.md` 의 `문의폼주소:` 에 붙여넣으세요.

---

## 7. 지도

`content.md` 의 `주소:` 를 정확히 입력하면 구글 지도가 자동으로 그 위치를 표시합니다. 별도 설정이 필요 없습니다.

---

막히는 부분이 있으면 그 화면을 저에게 알려주세요. 어디를 눌러야 하는지까지 안내해 드리겠습니다.
